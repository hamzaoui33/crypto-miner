import { useState, useCallback, useRef } from "react"; import { Pickaxe } from "lucide-react"; import { useGameState } from "@/hooks/useGameState"; import { triggerHaptic } from "@/utils/haptics"; const TapTab = () => { const { coins, taps, energy, maxEnergy, tapPower, tap, floatingTexts } = useGameState(); const coinRef = useRef<HTMLButtonElement>(null); const [isPressed, setIsPressed] = useState(false); const handleTap = useCallback( (e: React.MouseEvent<HTMLButtonElement>) => { const rect = coinRef.current?.getBoundingClientRect(); if (rect) { const x = e.clientX - rect.left; const y = e.clientY - rect.top; tap(x, y); } triggerHaptic('light'); setIsPressed(true); setTimeout(() => setIsPressed(false), 100); }, [tap] ); const energyPercent = (energy / maxEnergy) * 100; return ( <div className="flex flex-col items-center justify-center flex-1 gap-6 px-6 py-8 select-none relative overflow-hidden"> <div className="flex flex-col items-center gap-1"> <span className="text-gray-400 text-xs font-medium tracking-widest uppercase"> Your Coins </span> <div className="flex items-center gap-2"> <span className="text-5xl font-bold text-white tabular-nums tracking-tight"> {coins.toLocaleString()} </span> <span className="text-2xl pointer-events-none">🪙</span> </div> <span className="text-xs text-gray-500 mt-1"> {taps.toLocaleString()} total taps </span> </div> <div className="relative"> <button ref={coinRef} onClick={handleTap} className={`relative w-44 h-44 rounded-full flex items-center justify-center transition-transform duration-100 ease-out select-none ${ isPressed ? "scale-90" : "scale-100" } ${ energy <= 0 ? "opacity-40 cursor-not-allowed" : "cursor-pointer" }`} style={{ background: "radial-gradient(circle at 35% 35%, rgba(251,191,36,0.25) 0%, rgba(234,88,12,0.10) 60%, rgba(0,0,0,0) 100%)", border: "2px solid rgba(251,191,36,0.30)", boxShadow: "0 0 50px rgba(251,191,36,0.15), 0 8px 32px rgba(0,0,0,0.6), inset 0 2px 8px rgba(255,255,255,0.06)", touchAction: "manipulation", WebkitTapHighlightColor: "transparent", userSelect: "none", }} > <div className="absolute inset-2 rounded-full pointer-events-none" style={{ background: "radial-gradient(circle at 40% 30%, rgba(251,191,36,0.12) 0%, transparent 70%)", }} /> <Pickaxe className="w-16 h-16 text-amber-400 pointer-events-none" style={{ filter: "drop-shadow(0 0 14px rgba(251,191,36,0.5))", }} /> {floatingTexts.map((ft) => ( <span key={ft.id} className="absolute pointer-events-none font-bold text-amber-300 text-xl select-none" style={{ left: ft.x, top: ft.y, animation: "floatUp 900ms ease-out forwards", textShadow: "0 0 10px rgba(251,191,36,0.6)", }} > +{tapPower} </span> ))} </button> </div> <div className="w-full max-w-xs flex flex-col items-center gap-2"> <div className="flex items-center justify-between w-full"> <span className="text-xs text-gray-400 font-medium">⚡ Energy</span> <span className="text-xs text-gray-500 tabular-nums"> {energy.toLocaleString()}/{maxEnergy.toLocaleString()} </span> </div> <div className="w-full h-2.5 rounded-full bg-gray-800 overflow-hidden"> <div className="h-full rounded-full transition-all duration-200 ease-out" style={{ width: `${energyPercent}%`, background: energyPercent > 30 ? "linear-gradient(90deg, #f59e0b, #f97316)" : "linear-gradient(90deg, #ef4444, #f97316)", }} /> </div> <span className="text-[10px] text-gray-600"> Tap power: {tapPower.toLocaleString()} 🪙/tap </span> </div> <style>{` @keyframes floatUp { 0% { opacity: 1; transform: translateY(0) scale(1); } 60% { opacity: 0.8; transform: translateY(-50px) scale(1.2); } 100% { opacity: 0; transform: translateY(-90px) scale(0.8); } } `}</style> </div> ); }; export default TapTab;