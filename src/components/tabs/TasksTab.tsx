import { useState } from "react"; import { CheckCircle, MessageCircle, Twitter, Youtube, ExternalLink, Gift, Star, Trophy } from "lucide-react"; interface Quest { id: number; name: string; reward: number; icon: React.ElementType; status: "go" | "claim" | "claimed"; } const initialQuests: Quest[] = [ { id: 1, name: "Join our Telegram Channel", reward: 50000, icon: MessageCircle, status: "go" }, { id: 2, name: "Follow on X", reward: 50000, icon: Twitter, status: "go" }, { id: 3, name: "Subscribe to YouTube", reward: 50000, icon: Youtube, status: "go" }, { id: 4, name: "Invite 3 Friends", reward: 150000, icon: Gift, status: "go" }, { id: 5, name: "Reach Level 10", reward: 100000, icon: Trophy, status: "go" }, ]; const TasksTab = () => { const [quests, setQuests] = useState<Quest[]>(initialQuests); const handleGo = (id: number) => { setQuests((prev) => prev.map((q) => q.id === id ? { ...q, status: "claim" as const } : q )); }; const handleClaim = (id: number) => { setQuests((prev) => prev.map((q) => q.id === id ? { ...q, status: "claimed" as const } : q )); }; const totalRewards = quests .filter((q) => q.status === "claimed") .reduce((sum, q) => sum + q.reward, 0); return ( <div className="flex flex-col flex-1 gap-5 px-4 py-6 overflow-y-auto"> {/* Header */} <div className="flex flex-col items-center gap-1 pt-2"> <h1 className="text-xl font-bold text-white">Earn More Coins</h1> <p className="text-xs text-gray-500">Complete quests to earn rewards</p> </div> {/* Stats */} <div className="flex items-center justify-center gap-3"> <div className="px-4 py-2 rounded-xl bg-amber-500/10 border border-amber-500/20 text-center"> <p className="text-lg font-bold text-amber-400 tabular-nums">{totalRewards.toLocaleString()}</p> <p className="text-[10px] text-gray-500 uppercase tracking-wider">Claimed</p> </div> <div className="px-4 py-2 rounded-xl bg-emerald-500/10 border border-emerald-500/20 text-center"> <p className="text-lg font-bold text-emerald-400 tabular-nums"> {quests.filter((q) => q.status === "claimed").length}/{quests.length} </p> <p className="text-[10px] text-gray-500 uppercase tracking-wider">Completed</p> </div> </div> {/* Quests List */} <div> <h2 className="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-3 flex items-center gap-2"> <Star className="w-4 h-4 text-amber-400" /> Quests </h2> <div className="flex flex-col gap-2.5"> {quests.map((quest) => { const Icon = quest.icon; return ( <div key={quest.id} className="flex items-center gap-3 p-4 rounded-xl bg-white/5 backdrop-blur-md border border-white/10 transition-all" > <div className="w-10 h-10 rounded-lg bg-amber-500/10 flex items-center justify-center shrink-0"> <Icon className="w-5 h-5 text-amber-400" /> </div> <div className="flex-1 min-w-0"> <span className="text-sm font-medium text-white block">{quest.name}</span> <span className="text-xs text-amber-400/80 font-semibold">+{quest.reward.toLocaleString()} Coins</span> </div> {quest.status === "go" && ( <button onClick={() => handleGo(quest.id)} className="shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-xl text-xs font-bold bg-white/10 hover:bg-white/15 text-white border border-white/10 transition-all active:scale-95" > <ExternalLink className="w-3 h-3" /> Go </button> )} {quest.status === "claim" && ( <button onClick={() => handleClaim(quest.id)} className="shrink-0 px-4 py-2 rounded-xl text-xs font-bold bg-emerald-500 hover:bg-emerald-400 text-black transition-all active:scale-95 shadow-[0_0_16px_rgba(16,185,129,0.25)]" > Claim </button> )} {quest.status === "claimed" && ( <div className="shrink-0 flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-medium bg-emerald-500/10 text-emerald-400 border border-emerald-500/20" > <CheckCircle className="w-3.5 h-3.5" /> Done </div> )} </div> ); })} </div> </div> </div> ); }; export default TasksTab;