import { CheckCircle, Gift, Star, Target } from "lucide-react"; const dailyTasks = [ { id: 1, name: "Tap 100 times", reward: 50, progress: 67, total: 100, icon: Target }, { id: 2, name: "Use a boost", reward: 100, progress: 0, total: 1, icon: Star }, { id: 3, name: "Invite a friend", reward: 500, progress: 0, total: 1, icon: Gift }, ]; const achievements = [ { id: 1, name: "First 1,000 coins", reward: 200, completed: false }, { id: 2, name: "Reach level 5", reward: 1000, completed: false }, { id: 3, name: "10,000 total taps", reward: 5000, completed: false }, ]; const TasksTab = () => { return ( <div className="flex flex-col flex-1 gap-6 px-4 py-6 overflow-y-auto"> <div> <h2 className="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-3 flex items-center gap-2"> <CheckCircle className="w-4 h-4 text-emerald-400" /> Daily Tasks </h2> <div className="flex flex-col gap-2"> {dailyTasks.map((task) => { const Icon = task.icon; const progress = (task.progress / task.total) * 100; const isComplete = progress >= 100; return ( <div key={task.id} className="flex items-center gap-3 p-3 rounded-xl bg-gray-800/60 border border-white/5" > <div className={`w-10 h-10 rounded-lg flex items-center justify-center ${isComplete ? "bg-emerald-500/20" : "bg-gray-700/50"}`}> <Icon className={`w-5 h-5 ${isComplete ? "text-emerald-400" : "text-gray-400"}`} /> </div> <div className="flex-1 min-w-0"> <span className={`text-sm font-medium ${isComplete ? "text-emerald-400 line-through" : "text-white"}`}>{task.name}</span> <div className="flex items-center gap-2 mt-1"> <div className="flex-1 h-1.5 rounded-full bg-gray-700 overflow-hidden"> <div className={`h-full rounded-full transition-all ${isComplete ? "bg-emerald-500" : "bg-amber-500"}`} style={{ width: `${Math.min(progress, 100)}%` }} /> </div> <span className="text-[10px] text-gray-500 tabular-nums">{task.progress}/{task.total}</span> </div> </div> <span className="text-xs font-bold text-amber-400 whitespace-nowrap">🪙 {task.reward}</span> </div> ); })} </div> </div> <div> <h2 className="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-3"> Achievements </h2> <div className="flex flex-col gap-2"> {achievements.map((ach) => ( <div key={ach.id} className={`flex items-center gap-3 p-3 rounded-xl border transition-all ${ach.completed ? "bg-emerald-500/10 border-emerald-500/20" : "bg-gray-800/60 border-white/5"}`} > <div className={`w-8 h-8 rounded-full flex items-center justify-center ${ach.completed ? "bg-emerald-500/20" : "bg-gray-700/50"}`}> <Star className={`w-4 h-4 ${ach.completed ? "text-emerald-400" : "text-gray-500"}`} /> </div> <div className="flex-1"> <span className={`text-sm font-medium ${ach.completed ? "text-emerald-400" : "text-white"}`}>{ach.name}</span> </div> <span className="text-xs font-bold text-amber-400">🪙 {ach.reward.toLocaleString()}</span> </div> ))} </div> </div> </div> ); }; export default TasksTab;